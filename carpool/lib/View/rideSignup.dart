import 'package:flutter/material.dart';

class rideSignup extends StatelessWidget {
  const rideSignup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("RideSignup")),
    );
  }
}
